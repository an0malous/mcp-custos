#!/usr/bin/env sh
# Wire the compliance-citation check into your project's pre-commit.
# Written by custos-init (tokens substituted per install mode),
# then `chmod +x .husky/pre-commit`.

if {{CHECK_GUARD}}; then
  {{CHECK_RUN}}
else
  echo "compliance check skipped: {{CHECK_HINT}}"
fi
