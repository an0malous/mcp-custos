{
  "_comment": "Drop into your project's .claude/settings.json. Requires the mcp-custos npm package installed globally (bun add -g mcp-custos or npm i -g mcp-custos).",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "custos-precheck-edit"
          }
        ]
      }
    ]
  },
  "permissions": {
    "_comment": "Optional: prevent Claude from bypassing the pre-commit citation check. Humans can still --no-verify locally.",
    "deny": [
      "Bash(git commit*--no-verify*)",
      "Bash(git commit*-n *)",
      "Bash(git commit *-n)"
    ]
  }
}
